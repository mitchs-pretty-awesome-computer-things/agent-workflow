---
description: Plan a feature by running an open-ended grilling interview, then persist it as a GitHub epic with sub-tasks and blocker relationships.
---

Load the `grill-plan` skill and follow it.

The user invoked this command with:

$ARGUMENTS
